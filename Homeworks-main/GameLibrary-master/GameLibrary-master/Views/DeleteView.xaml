<UserControl x:Class="GameLibrary.Views.DeleteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GameLibrary.Views"
             mc:Ignorable="d" 
             MaxHeight="100" MaxWidth="600">
    <Grid Background="white">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Label Content="Input title: " FontFamily="Tahoma" FontSize="20" Margin="10"></Label>
        <TextBox FontSize="18" FontFamily="Tahoma" Margin="150 10 150 10" Foreground="Black" Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBox>
        <Button x:Name="SearchButton" Grid.Row="1" Margin="400 10 50 10" Content="Delete" Command="{Binding DeleteCommand}"/>
        <Button x:Name="backBUtton" Grid.Row="1" Margin="200 10 250 10" Content="Back" Command="{Binding NavigateHomeCommand}"/>
    </Grid>
</UserControl>
